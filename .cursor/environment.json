{
  "name": "Blackrose",
  "install": "bash .cursor/install.sh",
  "terminals": [
    {
      "name": "docs-dev",
      "command": "export PATH=\"$HOME/.local/bin:$HOME/.bun/bin:$PATH\" && cd docs && bun run docs:dev --host 127.0.0.1 --port 5173"
    }
  ]
}
